# 社区房屋租赁管理系统 · 需求与用例规约

> 版本：v1.1　日期：2026-09-14
> 说明：标注 `[已实现]` 表示已有代码/菜单/页面；`[本期新增]` 表示本期要开发；`[暂缓]` 表示本期不做、后续再迁。

---

## 1. 系统概述

面向**社区房屋租赁**的管理系统，覆盖「业主上传房源 → 审核上架 → 租客申请/签约 → 租金/水电账单 → 业主抄表 → 租客报修 → 业主处理维修 → 退租结算」的闭环。基于芋道 yudao（ruoyi-vue-pro）二次开发，单租户模式（`tenant.enable=false`）。

**参与角色（Actor）**

| 角色 | 说明 | 主要职责 |
|---|---|---|
| 管理员 | 物业/运营人员 | 房源审核、租客/合同/账单/退租/公告等后台全部操作、维修合格判定、水电读数审核 |
| 业主（房东） | 房源所有者 | 上传房源、设置水电计价、上传水电读数（带截图）、处理维修、维护个人资料、查看自己的房源/账单/维修 |
| 租客 | 租房的一方 | 提交租房申请、报修、确认维修完成、缴费、退租申请 |

**端（界面）划分**

| 端 | 面向 | 状态 |
|---|---|---|
| 管理端 | 管理员 | `[已实现]`，持续维护 |
| 业主端 | 业主 | `[本期新增]`（移动 H5） |
| 租客端 | 租客 | `[暂缓]` —— 租客的「报修」「确认维修完成」本期暂放在管理端由管理员代操作，后续迁到租客端 |

---

## 2. 功能需求

### 2.1 已实现功能（管理端）

#### ① 首页看板 `[已实现]`
- 综合数据：房源总数、出租率、在租合同数、待处理申请/退租/报修数、逾期账单等。
- 片区房源分布统计（按区域/小区聚合）。

#### ② 房源管理 `[已实现]`
- 房源增删改查 + 分页检索（小区、区域、状态等）。
- 房源编号自动生成：`FW+年月日时分秒`，唯一。
- 字段：小区、区域、楼栋、房号、户型、建筑面积、朝向、楼层/总层数、装修、月租金、押金、配套设施（JSON）、状态、描述、上架时间。
- 状态：`0-下架 / 1-上架 / 2-已锁定 / 3-已出租`；房东同意申请**不改变房源状态**，房源保持「上架」，直到有租客完成签约并缴清首期账单才置「已出租」（先到先得）。
- 房源图片管理：实景图 / 户型图，支持排序。

#### ③ 租客管理 `[已实现]`
- 租客实名信息增删改查；身份证唯一、手机号必填。
- 实名认证状态：`未认证 / 认证中 / 已认证 / 认证失败 / 已锁定`。

#### ④ 租房申请 `[已实现]`
- 编号 `SQ+年月日时分秒`；**租客在租客端发起，房东在业主端审批**（管理端不再提供该功能）。
- 字段：房源、租客、期望入住日期、租期（月）、付款方式、月租金、押金、租客备注；列表展示时补全小区/楼栋房号、房东姓名电话、租客姓名电话。
- 状态：`待审批 / 已通过 / 已驳回 / 已签约 / 已失效（原「已超时」）`。落选（房源被他人抢租）置为「已失效」并记录原因。
- 房东**同意**后自动生成待签署合同 + 首期账单（押金 + 首月租金）；同一房源可对多个租客同意，先完成「双方签署 + 缴清首期账单」者得。

#### ⑤ 合同管理 `[已实现]`
- 合同增删改查，编号 `HT-年月日时分秒`；续签、取消、即将到期查询（默认 60 天）。
- 状态：`待签署 / 待缴费 / 生效中 / 即将到期 / 退租处理中 / 已到期 / 已退租 / 已取消`。

#### ⑥ 租金/物业费账单 `[已实现]`
- 账单增删改查，编号 `ZD-年月日时分秒`；首期/周期账单；租金+物业费+滞纳金；缴费状态 `待缴费/已缴费/已逾期`。

#### ⑦ 抄表管理 `[已实现]`
- 录入抄表读数：自动取上期读数、校验本期 ≥ 上期、单价缺省取配置、算用量费用；确认生成水电账单（缴款日 = 抄表日 + 15 天）。
- 水/电单价、抄表周期可配置。

#### ⑧ 水电费账单 `[已实现]`
- 由抄表确认生成；水/电/水+电；缴费状态同租金账单。

#### ⑨ 缴费记录 `[已实现]`
- 统一记录三类账单（租金/物业费、水电费、退租结算）的缴费。

#### ⑩ 退租申请 `[已实现]`
- 申请增删改查；状态 `待处理 / 已处理`；处理 = 房屋验收 + 费用结算，生成结算单。

#### ⑪ 退租结算单 `[已实现]`
- 结算单查看；编号 `JS-年月日时分秒`；记录押金处理、剩余租金、各类欠费、抵扣、退还/补缴。

#### ⑫ 维修工单 `[已实现]`（状态机本期调整，见 2.4）
- 工单增删改查，编号 `WX-年月日时分秒`；优先级、评价、进度留痕。
- 现状状态：`待分配 / 待处理 / 处理中 / 待确认 / 已归档`。

#### ⑬ 公告管理 `[已实现]`
- 公告增删改查，编号 `GG-年月日时分秒`；富文本；分类；`草稿/已发布/已删除`；置顶；定向推送。

### 2.2 业主端功能 `[本期新增]`

> 业主端为移动 H5，业主（房东）注册/登录后使用。核心 4+1 个功能：

#### A. 上传房源（含水电计价设置）
- 业主填写房源基本信息（小区、区域、楼栋、房号、户型、面积、朝向、楼层、装修、月租金、押金、设施、描述、图片）。
- **水电计价规则**（上传房源时设置）：
  - **水费**：二选一 —— ① 统一单价；② 固定三档梯度（档一 0~X 吨、档二 X~Y 吨、档三 >Y 吨，各填单价）。
  - **电费**：固定时段峰谷两价 —— 峰时段（8:00–22:00）× 峰价、谷时段（22:00–8:00）× 谷价，业主填峰/谷两个单价。
- 提交后进入 **`待审核`**，管理员审核通过才上架。

#### B. 上传水电信息（附截图）
- 业主按月上传水电**读数 + 表盘截图**。
  - 水费：上传水表总读数（1 张截图），按三档梯度或统一价计费。
  - 电费（峰谷电表）：上传峰段读数、谷段读数（各附截图），分别按峰/谷价计费。
- 提交后进入 **`待审核`**，管理员审核读数/截图通过后生成水电费账单。

#### C. 处理维修请求（含上传处理证据）
- 业主查看自己房源的租客报修工单 → 上门处理 → **上传处理后证据（照片）**。
- 之后：**管理员判定维修合格** 或 **租客点击"维修完成"** → 工单变「已处理」。
- 管理员判「不合格」→ 工单**退回业主重做**（重新处理、重新传证据）。

#### D. 设置个人信息
- 业主维护个人资料（姓名、手机号、身份证/实名、紧急联系人等）。

#### E. 我的资产（只读）
- 「我的房源」：查看自己上传的房源及状态（待审核/上架/已出租等）。
- 「我的账单」：查看自己房源产生的租金/水电账单。
- 「我的维修」：查看自己房源的报修工单及处理状态。

### 2.3 合同模板 `[本期新增]`（管理端）

- 模板增删改查、类型（标准/短期/商业）、HTML 内容 + 变量占位符、版本、启停。
- 新建合同时可选模板填充。

### 2.4 关键业务规则

1. **房源审核**：业主上传房源 → `待审核` → 管理员审核通过才上架；不通过可退回业主修改。
2. **水电计价**：
   - 水费 = 统一单价，或三档梯度分段计费（用量 = 本期 − 上期读数）。
   - 电费（峰谷）= 峰段用量 × 峰价 + 谷段用量 × 谷价。
3. **水电抄表**：上期读数 = 最近一条本期读数；本期 < 上期时报错；业主上传读数+截图，管理员审核后生成账单（缴款日 = 抄表日 + 15 天）。
4. **维修三方流程**：租客报修（本期在管理端代录）→ 业主处理 → 业主上传证据 → 管理员判定合格 / 租客确认完成 → 已处理；不合格 → 退回重做。
5. **退租结算**：到期退租退还 = 押金 − 欠费抵扣；提前退租退还/补缴 = 剩余租金 − 欠费抵扣，押金不退（违约金）；欠费抵扣 = 物业费 + 水电费 + 维修费。
6. **合同签署与成交（模拟，先到先得）**：房东同意申请后生成合同 + 首期账单，**房源仍保持「上架」**。房东与租客双方各自「确认签约」，且租客缴清首期账单后，合同置「生效中」、房源置「已出租」。同一房源若有多份未生效合同，第一个满足条件的合同赢下房源，其余合同自动置「已取消」、对应申请置「已失效」（原因：该房源已被其他租客承租）；已缴的首期账单不自动退款，保留作为人工退款凭据。
7. **编号规则**：房源 `FW`、合同 `HT`、租金账单 `ZD`、水电账单 `SD`、申请 `SQ`、退租申请 `TZ`、结算单 `JS`、工单 `WX`、公告 `GG`，均以时间戳保证唯一。

### 2.5 数据模型要点

- 新增业主实体：复用 `member_user` + 业主扩展信息表（姓名、身份证、手机、紧急联系人等，类比现有 `rental_tenant_info`）。
- `rental_house` 增加 `owner_id` 关联业主；新增水电计价字段（水费计价方式 + 三档单价 / 电费峰价、谷价）。
- 水电读数/截图与抄表记录（`rental_meter_reading`）关联，增加截图与「待审核/已通过/已驳回」审核状态。
- 维修工单增加「处理证据（图片）」「验收/确认」相关字段，适配三方流程状态机。

### 2.6 非功能需求

- **技术栈**：JDK17 / Spring Boot / MyBatis-Plus / MySQL 8 / Redis / Vue3 + Element Plus（管理端）/ 移动 H5（业主端）。
- **权限**：RBAC + 菜单/按钮权限（`@PreAuthorize`）；业主端按 `owner_id` 隔离数据（只能看/管自己的房源、账单、维修）。
- **数据**：逻辑删除、公共字段、金额 DECIMAL、编号唯一约束。
- **单租户**：`tenant_id` 恒为 0。

---

## 3. 用例规约

### 3.0 用例总览

| 编号 | 用例名 | 参与者 | 状态 |
|---|---|---|---|
| UC-01 | 租房申请与审批 | 租客、房东（业主） | 已实现 |
| UC-02 | 合同生命周期管理 | 管理员 | 已实现 |
| UC-03 | 租金账单与缴费 | 管理员、租客 | 已实现 |
| UC-04 | 水电抄表与账单 | 业主、管理员 | 本期（业主上传待编码） |
| UC-05 | 退租与费用结算 | 租客、管理员 | 已实现 |
| UC-06 | 维修工单（三方） | 租客、业主、管理员 | 本期（业主处理待编码） |
| UC-07 | 公告发布与阅读 | 管理员、租客 | 已实现 |
| UC-08 | 业主上传房源与审核 | 业主、管理员 | 本期 |
| UC-09 | 业主设置个人信息 | 业主 | 本期 |
| UC-10 | 业主查看我的资产 | 业主 | 本期 |
| UC-11 | 房源管理（CRUD 归并） | 管理员 | 已实现 |
| UC-12 | 租客管理（CRUD 归并） | 管理员 | 已实现 |
| UC-13 | 公告管理（CRUD 归并） | 管理员 | 已实现 |
| UC-14 | 合同模板管理（CRUD 归并） | 管理员 | 本期 |

> 租客端相关用例（预约看房、收藏、公告阅读、租客注册实名）`[暂缓]`，本期不展开。

---

### UC-01 租房申请与审批

- **参与者**：租客（发起）、房东/业主（审批）
- **前置条件**：房源「上架」；租客已登录租客端。
- **基本流程**：
  1. 租客在租客端「找房」选房源，填期望入住日期、租期、付款方式、月租金、押金、备注，提交申请。
  2. 系统校验房源仍在架、该租客对此房源无进行中的申请；生成申请编号，状态「待审批」。
  3. 房东在业主端「租房申请」看到该申请（含房源地址、租客姓名与手机号）。
  4. 房东**驳回** → 「已驳回」+ 驳回原因（必填）；**同意** → 「已通过」。
  5. 同意后系统自动生成**待签署合同**与**首期账单**（押金 + 首月租金，7 天内缴清），**房源仍保持「上架」**。
  6. 进入签署与缴费流程（见 UC-02、UC-03）。
- **扩展流程**：
  - 3a. 房东超时未处理 → 「已失效」。
  - 3b. 申请提交后房源已被下架 → 提示房源不可申请。
  - 5a. 房东可对**同一房源**同意多个租客，各自生成独立合同与账单，先完成签约缴费者得。
- **业务规则**：房东只能审批**自己房源**的申请；同一申请不可重复审批。

### UC-02 合同生命周期管理

- **参与者**：房东/业主、租客（管理端可做 CRUD 兜底）
- **基本流程**：
  1. 合同由 UC-01 同意后自动生成（或在管理端新建），含房源、租客、租期、月租金、押金、付款方式。
  2. 初始「待签署」，**房源保持「上架」**。
  3. 模拟签署：房东「确认签约」+ 租客「确认签约」，且**首期账单已缴清**。
  4. 三个条件全部满足时合同置「生效中」、房源置「已出租」、申请置「已签约」——此步为**先到先得**，并发下只有一个合同能抢到房源。
  5. 落选合同自动置「已取消」，其来源申请置「已失效」。
  6. 后续流转：生效中 → 即将到期 → 已到期；续签延长期限 → 「生效中」；取消 → 「已取消」。

### UC-03 租金账单与缴费

- **参与者**：租客（缴费）、房东、管理员（缴费登记兜底）
- **基本流程**：
  1. UC-01 房东同意后，系统按合同自动生成**首期账单**：应缴 = 押金 + 首月租金，费用周期 = 起租日 ~ 起租日 + 1 个月 − 1 天，截止日 = 生成日 + 7 天。
  2. 租客在租客端「我的账单」缴费（现金/微信/支付宝/银行转账），或由管理员在管理端「缴费登记」代收。
  3. 缴费写入 `rental_payment_record` 留痕，账单置「已缴费」。
  4. 缴费会触发一次合同成交判定（见 UC-02 步骤 4）——缴费可能正是成交的最后一环。
- **业务规则**：同一账单不可重复缴费；租客只能缴自己的账单；滞纳金可减免留原因。

### UC-04 水电抄表与账单

- **参与者**：业主（上传读数+截图）、管理员（审核生成账单）
- **前置条件**：房源有「生效中」合同；已设置水电计价。
- **基本流程**：
  1. 业主上传水电读数 + 表盘截图（电费峰谷分别传峰/谷读数）。
  2. 系统校验本期 ≥ 上期，按计价规则（水梯度/电峰谷）算用量与费用。
  3. 管理员审核读数/截图 → 通过生成水电费账单（缴款日 = 抄表日 + 15 天）。
- **扩展流程**：2a. 本期 < 上期 → 报错；3a. 管理员可驳回读数，业主重新上传。

### UC-05 退租与费用结算

- **参与者**：租客（发起）、管理员（验收结算）
- **基本流程**：租客提交退租申请 → 管理员房屋验收、录入各项欠费/维修费/剩余租金 → 系统计算结算单 → 回写状态。
- **业务规则**：到期退租退还 = 押金 − 欠费抵扣；提前退租退还/补缴 = 剩余租金 − 欠费抵扣（押金不退）。正 = 退还，负 = 补缴。

### UC-06 维修工单（三方）

- **参与者**：租客（报修）、业主（处理）、管理员（判定）
- **前置条件**：租客在租。
- **基本流程**：
  1. 租客报修（类型、描述、图片、期望上门时间）——本期由管理员在管理端代录。
  2. 系统生成工单，状态「待处理」。
  3. 业主接单 → 「处理中」，上门处理。
  4. 业主处理完成，上传处理证据（照片）→ 状态「待验收」。
  5. 管理员判定合格 **或** 租客确认「维修完成」→ 状态「已处理」。
- **扩展流程**：5a. 管理员判「不合格」→ 退回业主「处理中」重做。

### UC-07 公告发布与阅读

- **参与者**：管理员（发布）、租客（阅读）
- **基本流程**：新建公告（标题、分类、富文本、定向范围、置顶）→ 草稿/发布 → 租客查看。

### UC-08 业主上传房源与审核

- **参与者**：业主（上传）、管理员（审核）
- **基本流程**：
  1. 业主填房源信息 + 水电计价（水费统一价或三档梯度；电费峰谷两价）+ 图片。
  2. 提交 → 状态「待审核」。
  3. 管理员审核：通过 → 「上架」；驳回 → 退回业主修改。
- **业务规则**：只有审核通过的房源才对外可见/可被申请。

### UC-09 业主设置个人信息

- **参与者**：业主
- **基本流程**：维护姓名、手机、身份证、紧急联系人等；身份证唯一。

### UC-10 业主查看我的资产

- **参与者**：业主
- **基本流程**：查看「我的房源」（状态）、「我的账单」（租金/水电）、「我的维修」（工单状态），均为只读。

### UC-11~UC-14 管理类用例（CRUD 归并）

| 编号 | 用例 | 参与者 | 说明 |
|---|---|---|---|
| UC-11 | 房源管理 | 管理员 | 图片、上/下架、审核业主上传房源、编号生成 |
| UC-12 | 租客管理 | 管理员 | 实名信息、身份证唯一、认证状态 |
| UC-13 | 公告管理 | 管理员 | 发布/草稿/置顶 |
| UC-14 | 合同模板管理 | 管理员 | 模板 CRUD、变量占位符、版本、启停 |

---

## 4. 已确认决策

1. **端与角色**：三角色（管理员/业主/租客）、三端（管理端/业主端/租客端）。本期新增业主端，租客端暂缓（租客报修/确认完成暂由管理端代操作，后续迁移）。
2. **合同签署与成交**：模拟签署——房东与租客各自确认，且首期账单缴清后合同才生效。房源在签约期间**保持「上架」**（房东可同时与多个租客签约），成交时**先到先得**：第一个满足条件的合同占用房源，其余合同自动取消。
3. **房源上传**：业主上传 → 管理员审核通过才上架。
4. **水电计价**：水费固定三档梯度或统一价（二选一）；电费固定时段峰谷两价（峰 8–22、谷 22–8）。
5. **水电抄表**：业主上传读数+截图 → 管理员审核后生成账单。
6. **维修流程**：租客报修 → 业主处理传证据 → 管理员判定合格 / 租客确认完成 → 已处理；不合格退回重做。
7. **文档格式**：采用本文档风格，不套用课程模板。
